#include <stdio.h>

// char* welcome() {
//     return "Welcome to Code Radar!";
// }

int main() {
    // printf("%s", welcome());
    int cp sp;
    int diff = sp - cp;
    scanf("%d %d", &cp, &sp);
    if(doff> 0){
        printf("Profit");
    }else if(diff < 0){
        printf("Loss");
    }else {
        printf(" No Profiit No Loss");
    }
    return 0;
}